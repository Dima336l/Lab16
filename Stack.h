#ifndef STACK_H
#define STACK_H

#include "Node.h"
#include <bits/stdc++.h>

template <typename T>

class Stack {
private:
  Node* top;
public:
  Stack () { top = NULL; }
  void push( T item) {
    Node* node = new Node (item);
    node->
  }
}



#endif
